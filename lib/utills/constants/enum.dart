/// list of Enums
/// they cannot be created inside a class

enum TextSIzes { small, medium, large }

enum OrderStatus { processing, shipped, deliverd, pending, cancelled }

enum PaymentMethods {
  paypal,
  googlePay,
  applePay,
  visa,
  masterCard,
  creditCard,
  paystack,
  razorPay,
  paytm
}
